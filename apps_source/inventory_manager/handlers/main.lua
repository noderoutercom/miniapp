-- handlers/main.lua — primary stateful workflow
local M = {}

-- function M.list_items(params)
--   local rows, err = noderouter.query("list_items", json.encode(params))
--   if err then return nil, err end
--   return rows
-- end

return M
